# Howpsuzlyk: Gizlin sazlamalar we API açarlar
.env

# Maglumatlar: Okuwçy işjeňligi we CSV faýllary
*.csv
data/

# Wagtlaýyn faýllar we loglar
*.log
git rm -r --cached .env
git rm -r --cached *.csv
git add .gitignore
git commit -m "Howpsuzlyk sazlamalary goşuldy"
